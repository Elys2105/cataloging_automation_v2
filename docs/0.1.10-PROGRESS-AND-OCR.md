# 0.1.10 - Full-profile progress and conservative Vietnamese repair

- `Chạy toàn bộ` now displays every inventory row from page 1 to the last page.
- Rows already completed on the website are shown in order and skipped safely.
- The UI displays the website inventory position instead of the internal SQLite ID.
- Final verification with review rows no longer raises a fatal popup; it logs a warning.
- Conservative OCR normalization repairs title variants such as
  `KET LUAN CUA BAN THUONG VU PANG UY` without running broad spell correction
  over names and free text.
