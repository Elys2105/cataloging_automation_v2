# 0.1.24 - Unhinted weak-header rescue

Fixes a regression where records with empty/stale document type and symbol metadata skipped the focused document-header OCR pass. Weak generic OCR now triggers header crop recovery both for fresh OCR and cached OCR. This restores handwritten document-number recovery and bounded abstract extraction without requiring a pre-existing CV hint.
