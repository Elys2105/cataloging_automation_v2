# 0.1.23 - Công văn OCR garbage and handwritten number guard

- Reject recurring OCR prefix debris that turns handwritten 234 into 56234.
- Truncate visible OCR garbage suffixes such as `nu mm | ^ E` and `mmm`.
- Rank complete Vietnamese Công văn titles above short corrupted alternatives.
- Keep existing report and other document-type pipelines unchanged.
