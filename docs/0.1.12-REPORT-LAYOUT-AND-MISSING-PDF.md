# 0.1.12

- Special parser for landscape/daily Báo cáo title blocks immediately below `Số ...-BC/...`.
- Normal Báo cáo parser remains unchanged.
- `BC/...` always maps the document type to `Báo cáo`, overriding an existing `Khác` fallback.
- Records with `Số văn bản = Không xác định` and viewer `0 trên 0` are marked `skipped` without OCR or submit.
